# 更新日志 (Changelog)

所有值得注意的改动都记录在此文件。

本文件遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的书写习惯。
版本号唯一来源：`app/build_info.py` 中的 `APP_VERSION`。

## [1.1.1] - 2026-09-17

本次为主要功能更新，新增离线文件分析、中英文界面切换与版本图标。

### 新增

- **离线文件分析**：日志页新增「打开文件与离线分析」。支持 CSV（UTF-8/GB18030）、TSV、XLSX、JSON、ASC，单文件最大 20 MiB / 10 万条记录。按通道、ID、标准/扩展、方向分组统计帧数、相邻报文周期、数据变化、时间倒退与逐字节范围。无需连接 CAN 即可离线分析，结果可导出 JSON 报告。
  - 说明文档：`docs/file-analysis.md`
- **中英文界面**：顶部一键切换 `中文 / English`，不刷新页面、不改变输入值/文件路径/规则名/报文数据或正在运行的任务；同步支持深色/浅色/跟随系统主题，偏好保存在浏览器本地。界面词典集中在 `web/i18n.js`。
  - 说明文档：`docs/frontend-workspace.md`
- **版本与图标**：新增 `/api/version` 接口（返回版本号与前端构建标识）；状态接口同步返回版本号；页面与 Windows EXE 现带有应用图标（`web/icon.ico`，含 16~256px 多尺寸，源文件 `web/icon.svg`）。
  - 版本与图标规范见 `docs/versioning.md`
- **触发器/文件选择增强**：触发回应支持 `send_extended`；文件选择新增「选择文件夹」模式、明确校验模式值；保存对话框启用覆盖确认。

### 变更

- 新增依赖 `openpyxl>=3.1`（用于 XLSX 离线分析）。
- 前端页面布局重构：改为侧栏导航 + 标签式分组 + 独立横向滚动表格；720px 以下自动转为顶部导航；支持键盘方向键/Home/End 切换页面（详见 `docs/frontend-workspace.md`）。

### 修复

- 更新触发器时对 `send_extended` 的布尔解析更健壮，并捕获 `TypeError` 返回 400 而非 500。
- 文件选择模式对非法输入返回明确报错。

### 测试

- 测试由 33 增至 **59**：新增离线文件分析、文件选择、触发器编辑/逻辑、i18n（含触发 DOM）、版本图标等 pytest 及 DOM/CJS 检查用例。